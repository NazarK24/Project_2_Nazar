[
    {
      "name": "backend-redis",
      "image": "${BACKEND_REDIS_IMAGE_URL}",
      "cpu": 256,
      "memory": 512,
      "portMappings": [
        {
          "containerPort": 8002,
          "hostPort": 8002
        }
      ],
      "environment": [
        { "name": "REDIS_PASSWORD", "value": "${REDIS_PASSWORD}" }
      ],
      "essential": true
    }
  ]
  